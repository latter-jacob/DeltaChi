<h2>Edit Exec</h2>

<%= form_for @exec, :url => {:action => "update"} do |f| %>

  <%= f.label :name %>
  <%= f.text_area :name %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :image %>
  <%= f.file_field :file_name %>

  <%= f.submit %>
<% end %>
