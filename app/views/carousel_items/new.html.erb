<h2>Add a new slide</h2>
<%= form_for @carousel_item, :url => {:action => "create"} do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.label :image %>
  <%= f.file_field :file_name %>

  <%= f.submit %>
<% end %>
